const express = require('express');
const doctorController = require('../controllers/doctorController');

const router = express.Router();

// ... similar routes for doctor CRUD operations

module.exports = router;